<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Web</title>
    <link rel="stylesheet" href="ci-icon/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(to right, #081b29, #0a3b60);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .wrapper {
            display: flex;
            width: 60%;
            max-width: 1200px;
            height: 80vh;
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .form {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 40px;
            background-color: #f4f6f8;
        }

        .form h1 {
            font-size: 48px;
            color: #333;
            margin-bottom: 20px;
        }

        .input-group {
            margin-bottom: 30px;
            position: relative;
        }

        .input-group input {
            width: 100%;
            padding: 15px 40px;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 16px;
            background-color: #fff;
            color: #333;
            transition: border-color 0.3s;
        }

        .input-group input:focus {
            border-color: #0ef;
            outline: none;
        }

        .input-group i {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #0ef;
        }

        .submit {
            width: 100%;
            padding: 15px;
            background: linear-gradient(45deg, #0ef, #c800ff);
            border: none;
            border-radius: 10px;
            font-size: 20px;
            color: white;
            cursor: pointer;
            transition: background 0.3s;
        }

        .submit:hover {
            background: linear-gradient(45deg, #c800ff, #0ef);
        }

        .footer {
            margin-top: 20px;
            text-align: center;
            color: #666;
        }

        .footer a {
            color: #0ef;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        .banner {
            flex: 1;
            background: linear-gradient(to right, #0ef, #c800ff);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-end;
            padding: 40px;
            color: white;
            text-align: right;
        }

        .banner h1 {
            font-size: 60px;
            line-height: 1.2;
            margin-bottom: 20px;
        }

        .banner p {
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="wrapper">
        <div class="form">
            <h1>Inicio</h1>
            <div class="input-group">
                <i class="fa-solid fa-user"></i>
                <input type="text" name="username" placeholder="Usuario">
            </div>
            <div class="input-group">
                <i class="fa-solid fa-lock"></i>
                <input type="password" name="password" placeholder="Contraseña">
            </div>
            <button class="submit">Iniciar Sesión</button>
            <p class="footer">No tienes cuenta? <a href="#">Por favor, Regístrate</a></p>
        </div>
        <div class="banner">
            <h1>¡Bienvenido!</h1>
            <p>Gracias por visitar nuestro sistema, por favor inicie sesión.</p>
        </div>
    </div>
</body>
</html>
